# Papalicious Admin Panel — Environment Variables
# Copy this to .env.local for local development

# Your deployed server URL on Vercel (e.g. https://papalicious-server.vercel.app/api)
# Leave empty during local development — vite proxy handles it automatically
VITE_API_URL=
